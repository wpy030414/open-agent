# PRD — Open Agent 产品需求文档

## 产品定位

Open Agent 是一个**轻量级、可自托管的 Web AI 智能体平台**。它的核心价值是让个人或团队能够快速部署一个属于自己的 AI 助手，通过插件扩展能力、通过技能注入知识、通过文件附件实现多模态交互，并通过管理员面板灵活配置一切。

## 目标用户

- **个人开发者**：想要一个自托管的 AI 聊天界面，对接各种 OpenAI 兼容的模型 API
- **小团队**：需要一个内部的 AI 助手，能通过插件接入内部工具和数据
- **AI 应用开发者**：需要一个可快速迭代的 AI Agent 原型平台

## 核心用户场景

### 场景 1：登录与日常对话

用户打开网页 → 输入用户名 → 输入 4 位 PIN 登录 → 开始与 AI 对话 → 看到流式输出的回复 → 点击建议继续追问。

### 场景 2：使用插件工具

用户提问涉及外部数据 → AI 自动调用插件工具（如搜索、查询数据库）→ 工具结果返回 → AI 基于结果生成最终回复。

### 场景 3：发送文件附件

用户在输入框附加文件（图片、Excel、PDF 等）→ 文件上传并解析 → AI 基于文件内容进行多模态回答（如识图、分析表格、解读 PDF）。

### 场景 4：管理员配置

管理员点击设置图标 → 输入管理员密钥 → 修改模型配置/系统提示词 → 上传新插件/技能 → 配置立即生效，无需重启。

### 场景 5：对话管理

用户在侧边栏查看所有对话历史 → 切换对话 → 重命名或导出对话 → 从某条消息回退重新对话 → 删除对话。

### 场景 6：管理员数据浏览

管理员进入统计面板 → 查看用户数/对话数/消息数 → 浏览所有对话列表 → 展开查看具体对话的消息内容。

## 功能需求

### F1：用户认证

- 用户名 + 4 位数字 PIN 登录
- 首次使用自动引导设置 PIN
- PIN 使用 PBKDF2 安全哈希存储
- JWT Token 30 天有效，免重复登录
- 支持修改 PIN（需验证旧 PIN）
- 登出功能

### F2：聊天界面

- 流式输出（token 逐字显示）
- 思考过程展示（可折叠，支持 AI 扩展推理模式）
- 工具调用展示（调用名 + 输入参数 + 结果摘要）
- 文件附件展示（图片预览、文件卡片、下载）
- Mermaid 图表渲染
- 后续建议（3 条可点击的追问）
- 中断/取消正在进行的对话
- 消息回退（从任意消息处删除并重新对话）

### F3：对话管理

- 创建新对话
- 查看对话列表（按更新时间排序）
- 切换对话（加载历史消息）
- 重命名对话
- 删除对话（级联删除消息）
- 导出对话为 TXT 文件
- URL 哈希路由（`#/c/{id}`）支持浏览器前进后退

### F4：插件系统

- 插件清单（plugin.json）定义工具
- 插件模块（index.ts/js/mjs）实现工具逻辑
- AI 通过 function calling 自动调用工具
- 管理员可上传/卸载插件（zip 包）
- 用户可在侧边栏查看所有可用插件和工具
- 支持手动调用插件工具

### F5：技能系统

- SKILL.md 文件定义技能（YAML 前置元数据 + Markdown 内容）
- 技能内容自动注入系统提示词
- 管理员可上传/卸载技能（zip 包）

### F6：文件附件

- 支持上传图片（→ base64 多模态）、Excel（→ CSV）、PDF（→ 文本）、纯文本文件
- 输入框附件上传按钮，可切换开关
- 管理员可全局开关附件功能（`support_attachments` 配置）
- 上传限制 20MB，文件以 UUID 重命名存储
- 附件信息持久化到消息记录

### F7：管理员面板

- 密钥认证 → JWT（24h 有效期）
- 品牌配置（应用名称、Favicon、聊天背景图）
- 模型配置（API 地址、密钥、模型名称）
- 系统提示词编辑
- 插件管理（列表/上传/卸载）
- 技能管理（列表/上传/卸载）
- 统计面板（用户数/对话数/消息数 + 对话浏览）
- API Key 显示（⚠️ 当前接口返回明文，脱敏为未实现的已知缺陷，见 specs/module-admin.md）

### F8：国际化

- 中文（默认语言）
- 英文
- 语言偏好持久化到 localStorage

### F9：主题

- 浅色/深色模式
- 跟随系统偏好
- 偏好持久化到 localStorage

## 非功能需求

### NF1：部署简单

- 单进程部署，无需外部数据库
- `pnpm build && pnpm start` 即可运行
- SQLite 单文件存储

### NF2：兼容性

- 支持所有 OpenAI 兼容的 API（包括本地模型、第三方代理）
- 支持 thinking/reasoning 内容流式输出（如 DeepSeek、Qwen、DashScope）
- 支持多模态内容（图片 base64）

### NF3：可靠性

- 客户端自动重试（最多 3 次，指数退避）
- SSE 15 秒心跳保活
- 60 秒空闲超时

### NF4：安全性

- 管理员密钥不暴露给前端
- PIN 使用 PBKDF2（SHA-512、10000 次迭代、随机盐）安全哈希
- JWT 认证保护管理员和用户端点
- Zip slip 攻击防护
- 对话按用户隔离
- 时序安全的 PIN 比较（`timingSafeEqual`）
